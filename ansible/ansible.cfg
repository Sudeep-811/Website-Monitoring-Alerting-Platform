[defaults]
inventory = inventory.ini
remote_user = ec2-user
private_key_file = ~/.ssh/monitoring-platform-key
host_key_checking = False
retry_files_enabled = False

[ssh_connection]
ssh_args = -o StrictHostKeyChecking=no
